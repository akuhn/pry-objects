require 'spec_helper'

describe Pry::Objects do
  it 'has a version number' do
    expect(Pry::Objects::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
